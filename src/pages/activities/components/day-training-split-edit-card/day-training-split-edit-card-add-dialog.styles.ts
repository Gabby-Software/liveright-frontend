import styled from 'styled-components'

import Dialog from '../../../../components/dialogs/dialog/dialog.component'

export const DialogStyles = styled(Dialog)`
  .DaySplitEditCard__control {
    &-checkBtn {
      margin-top: 20px;
      width: 100%;
    }
  }
`
