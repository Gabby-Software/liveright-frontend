import React, {useState, useEffect, FC} from 'react';
import Styles from './chat-header-desktop.styles';

type Props = {};
const ChatHeaderDesktop: FC<Props> = ({}) => {
    return (
        <Styles>
        </Styles>
    );
};

export default ChatHeaderDesktop;
